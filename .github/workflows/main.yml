# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!
        
      - name: action-zip
        uses: montudor/action-zip@v1.0.0
        with:
          args: zip -qq -r extension.zip src

      - name: Chrome Addon Action
        # uses: trmcnvn/chrome-addon@7fc5a5ad3ff597dc64d6a13de7dcaa8515328be7
        uses: trmcnvn/chrome-addon@v2
        with:
          extension: 'ilnbgidmlamnmgaoopfpgnedckibllhd'
          zip: './extension.zip'
          client-id: '814880299974-j23qt8slp5mus3n8j7a1vqgpr8tkuqvq.apps.googleusercontent.com'
          refresh-token: '1//09OE_8vywbrUPCgYIARAAGAkSNwF-L9IrFoQ8cxjblxqmADtt5MfJAVxDJgZ1w9dZS4cGwhC4Uyc157ftxMMjkypjFsujrYWHhJM'
          #client-secret: '4/1AX4XfWhTZlgmmzczBX62gFLEgpb4UyxJ_3LG2GfImyV3vZxizrFy9UXc2pc'
